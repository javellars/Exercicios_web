function toggleMenu() {
    var menuLateral = document.getElementById('menuLateral');
    menuLateral.style.transform = menuLateral.style.transform === 'translateX(0%)' ? 'translateX(-100%)' : 'translateX(0%)';
}